apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false # Set to false for provisioned datasources
    uid: prometheus-cliptozip # Assign a unique ID
